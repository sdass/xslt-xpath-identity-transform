command xsltproc -o dest-2.xml  transform3.xsl db.changelog-1.8.xml

From the root find and select and copy the whole node (tree) including attributes and subnote recursively, whose changeset id in ( '1.8.4' or '1.8.6')